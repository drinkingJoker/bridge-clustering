# 服务器配置
server.port=8082

# 上下文路径
server.servlet.context-path=/api

# 文件上传配置
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# 临时文件存储路径
app.upload.dir=./uploads
app.python.script.dir=./python-scripts